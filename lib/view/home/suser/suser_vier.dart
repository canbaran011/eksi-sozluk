

import 'package:flutter/material.dart';

class SuserView extends StatelessWidget {
  const SuserView({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      alignment: Alignment.center,
      child: Text('SUSER VIEW'),
    );
  }
}